# ============================================================
# InsightMesh AI — Python dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# Recommended: use a virtualenv (you have `myenv/`).
#
# After installing, also run:
#   python -m spacy download en_core_web_sm
# ============================================================
sentencepiece

# --- Core web framework ---
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9       # required for file uploads (UploadFile)
pydantic>=2.5.0,<3.0
python-dotenv>=1.0.0

# --- HTTP clients ---
httpx>=0.27.0
requests>=2.31.0

# --- Data wrangling ---
pandas>=2.1.0
numpy>=1.24.0
python-dateutil>=2.8.0

# --- NLP & embeddings ---
transformers>=4.35.0
torch>=2.0.0                  # transformers requires; CPU-only is fine for dev
sentence-transformers>=2.5.0
keybert>=0.8.0
spacy>=3.7.0,<4.0
bertopic>=0.16.0
hdbscan>=0.8.33
scikit-learn>=1.3.0
langdetect>=1.0.9

# --- LLM (OpenAI v1 SDK) ---
openai>=1.10.0

# --- Forecasting ---
prophet>=1.1.4

# --- CSV profiling ---
ydata-profiling>=4.6.0

# --- Scrapers ---
praw>=7.7.0
google-api-python-client>=2.100.0

# ============================================================
# Optional: dev/test/quality tools  (uncomment to install)
# ============================================================
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# pytest-cov>=4.1.0
# httpx-mock>=0.10.0
# ruff>=0.3.0
# black>=24.0.0
# mypy>=1.9.0
